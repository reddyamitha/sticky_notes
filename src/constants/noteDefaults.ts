/** Default dimensions for new notes (pixels) */
export const DEFAULT_NOTE_WIDTH = 200;

/** Default dimensions for new notes (pixels) */
export const DEFAULT_NOTE_HEIGHT = 160;

/** Initial position offset for new notes (pixels) */
export const INITIAL_NOTE_X = 40;

/** Initial position offset for new notes (pixels) */
export const INITIAL_NOTE_Y = 40;

/** Offset between stacked notes when creating multiple in same area (pixels) */
export const NOTE_STACK_OFFSET = 20;

/** Minimum note dimensions when resizing (pixels) */
export const MIN_NOTE_WIDTH = 120;

/** Minimum note dimensions when resizing (pixels) */
export const MIN_NOTE_HEIGHT = 90;
